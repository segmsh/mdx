import Box from "place";

<div>
  Here’s an expression:
</div>

<table>
  <thead>
    <tr>
      <th>Rule</th>
      <th>Rationale</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>touch-action</code> is prohibited.</td>
      <td>An ad that can manipulate <code>touch-action</code> can interfere with
        the user's ability to scroll the host document.</td>
    </tr>
  </tbody>
</table>

import Box from "place";

{
  1 + 1 /* } */
}

Which you can also put inline: {1+1}. {2+3}

<Box title={"title"} props={"test"}>
  <Box>
    - Lists, which can be indented.
  </Box>
</Box>
